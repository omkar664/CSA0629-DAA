#include<stdio.h>
int main()
{
    int n,i,j;
    printf("Enter no.of elements required");
    scanf("%d",&n);
    for(i=1;i<n;i++)
    {
        for(j=1;j<=i;j++)
        {
            printf("%d",j);
        }
        printf("\n");
    }
}
 
